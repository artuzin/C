#include <stdio.h>

int main(void){
    int contador=1; //variavel de controle de loop

    while (contador <=5){
        printf("A variavel contador possui nesta iteracao o valor de %d\n", contador);

        contador++;
    }
    return(0);
 }   